@model IEnumerable<Pruebaunoparcial.Models.Empleo>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Cargo)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Empresa)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Fecha_ini)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Fecha_fin)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Mediador)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cargo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Empresa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha_ini)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha_fin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mediador)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EmpleoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EmpleoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EmpleoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
